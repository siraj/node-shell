# node-shell
```bash
docker build -t node_dev -f Dockerfile .
 docker run -it --rm -v $(pwd):/app  node_dev


  TradeHistory
    ✔ should add trades correctly
    ✔ should calculate total volume correctly


  2 passing (5ms)

```
